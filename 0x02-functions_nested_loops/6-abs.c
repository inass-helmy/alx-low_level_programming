#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @n: n is an integer
 *
 * Return: n if n >= 0 and -n otherwise
 *
*/
int _abs (int n)
{
	if ( n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}

}
